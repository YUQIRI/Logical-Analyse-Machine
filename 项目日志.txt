下位机：

扫描：
	初始化：初始化环形缓冲区、启动 DMA_IDLE 接收中断

	接收：DMA IDLE 中断模式处理上位机数据
	发送：创建4字节数组buffer（32位）构建大字节序--上传buffer（不用DMA）

2025\12\17
	放弃使用CLion，用keil编写，
	完成了上位机扫描下位机，下位机用DMA_IDLE接收，
	用环形缓冲区防止数据丢失

2025\12\21
	软件模拟采样数据，看是否能将数据上传给上位机
	设置采样、设置阶段
	设置多字节接收操作
	解引用操作符 * 的使用：连续读取并转换
		如triggerMask = *(uint32_t * )(cmd_buf + 1);

2026\2\28
	final:精确计量时间，通过改变读GPIO/移位/累加/Tick中断处理/NOP指令等
	汇编语言的数量来修改每个脉冲的时间
	以此来改变逻辑分析仪的工作频率
	但由于stm32c8t6的内存只有20K，过高的频率（9Mhz）只能储存0.002s数据
	因此要提高内存使用效率，不变的数据不再保存

2026\3\9
	Q1：由于中断回调出现问题，扫描不到下位机名称
	A1：修改中断回调条件
	
	Q2：引脚收不到数据
	A2：修改接收条件，使引脚随时间存储数据

	Q3：很多个数据表示同一个电平，导致很多个采样点只能表示几个电平变化
	A3：增加跳变点缓冲区和存储数据的判断条件，电平不变就不存储数据，但目前还没解决
	
2026\3\12
	成功让每一个点记录不同的电平变化，基本采样功能实现
	下一步要提高采样率到2Mhz并且要
	使用usb串口、使用汇编来提高采样率 
	
2026\3\14
	改进功能，
	利用汇编实现2Mhz采样，
	实现了USB传输，传输速率更快
	可在usb与串口传输之间切换
	可使用RLE上传模式，有一串相同数据可以只由两字节表示，上传速率更快
	但RLE需要最高位置零，损失一条采样通道