下位机：

扫描：
	初始化：初始化环形缓冲区、启动 DMA_IDLE 接收中断

	接收：DMA IDLE 中断模式处理上位机数据
	发送：创建4字节数组buffer（32位）构建大字节序--上传buffer（不用DMA）

2025\12\21
	软件模拟采样数据，看是否能将数据上传给上位机
	设置采样、设置阶段
	设置多字节接收操作
	解引用操作符 * 的使用：连续读取并转换
		如triggerMask = *(uint32_t * )(cmd_buf + 1);